import 'package:flutter/material.dart';

class NavBarMenuFeature extends StatelessWidget {
  const NavBarMenuFeature({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: const Center(
        child: Text("Menu"),
      ),
    );
  }
}
