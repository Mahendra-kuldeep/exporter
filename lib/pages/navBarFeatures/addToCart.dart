import 'package:flutter/material.dart';

class AddToCartFeature extends StatelessWidget {
  const AddToCartFeature({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: const Center(
        child: Text("add to cart"),
      ),
    );
  }
}
